#!/usr/bin/env bash

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
"$repo_root/scripts/aois_checkpoint.sh" \
  --source post-checkout \
  --next "Resume from latest reader paragraph or selected branch state." >/dev/null 2>&1 || true
