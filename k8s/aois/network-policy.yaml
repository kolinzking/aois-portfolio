apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: aois-api-local-only
  namespace: aois
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: aois-api
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 8006
  egress: []
