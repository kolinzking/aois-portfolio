apiVersion: v1
kind: Service
metadata:
  name: aois-api
  namespace: aois
  labels:
    app.kubernetes.io/name: aois-api
    app.kubernetes.io/part-of: aois-portfolio
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aois-api
  ports:
    - name: http
      port: 8006
      targetPort: 8006
