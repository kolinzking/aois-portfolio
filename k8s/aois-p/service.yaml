apiVersion: v1
kind: Service
metadata:
  name: aois-p-api
  namespace: aois-p
  labels:
    app.kubernetes.io/name: aois-p-api
    app.kubernetes.io/part-of: aois-portfolio
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aois-p-api
  ports:
    - name: http
      port: 8006
      targetPort: 8006
