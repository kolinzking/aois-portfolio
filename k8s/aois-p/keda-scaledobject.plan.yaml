apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: aois-p-api
  namespace: aois-p
spec:
  scaleTargetRef:
    name: aois-p-api
  minReplicaCount: 1
  maxReplicaCount: 1
  pollingInterval: 30
  cooldownPeriod: 300
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "70"
