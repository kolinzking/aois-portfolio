# v13.5 Next Version Bridge

Authoring status: authored

## What This Version Unlocks

`v13.5` unlocks GPU infrastructure reasoning.

AOIS can now explain how GPU resources become visible to the scheduler and observable to operators.

## Why The Next Version Exists

`v14` moves from infrastructure readiness to high-throughput inference serving.

Once AOIS understands GPU scheduling, it can reason about serving efficiency, concurrency, batching, and throughput.
