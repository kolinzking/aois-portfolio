# v5 Looking Forward

Authoring status: authored

## What You Should Carry Forward

- inspect before provider calls
- redact before logs
- never commit real secrets
- prompt-injection is input risk
- provider calls remain gated
- pattern checks are useful but incomplete

## What The Next Version Will Build On

Phase 3 will build infrastructure and GitOps foundations.

The security controls from `v5` must remain visible as AOIS moves toward clusters and deployment.
