# v0.7 Next Version Bridge

## What This Version Unlocks

You now understand what the model can do and why its output still needs system support.

## Why The Next Version Exists

`v0.8` adds the first durable data layer so AOIS can store incident and analysis records instead of forgetting everything after each run.
