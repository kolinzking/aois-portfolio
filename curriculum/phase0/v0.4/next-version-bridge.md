# v0.4 Next Version Bridge

## What This Version Unlocks

You now understand the protocol surface that later Python code will interact with.

That means `v0.5` does not have to teach HTTP and Python at the same time.

## Why The Next Version Exists

AOIS cannot stay as shell scripts forever.
The next step is to build typed Python foundations:

- environment loading
- structured data models
- predictable project setup

That is the layer later FastAPI and AI logic will sit on.
