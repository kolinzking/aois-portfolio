# v0.4 Looking Forward

## What You Should Carry Forward

- HTTP problems have layers
- `curl` is a first-class engineering tool, not a one-off command
- headers, status, and body should be inspected intentionally

## What The Next Version Will Build On

`v0.5` takes you from shell-level protocol work into Python service foundations.

That is where AOIS starts becoming a real application substrate instead of a collection of useful terminal tools.
