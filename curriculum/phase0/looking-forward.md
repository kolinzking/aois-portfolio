# Looking Forward

This file exists to preserve continuity between versions without forcing premature architecture.

## Expansion Rule

Only add new top-level system areas when the curriculum reaches them and the repo has real code or configs to place there.

Likely later additions include:

- `app/`
- `tests/`
- `tools/`
- `workflows/`
- `graph/`
- `policy/`
- `otel/`
- `k8s/`

## Memory Rule

For now, continuity lives in:

- `README.md`
- `curriculum/phase0/`
- Git history

Broader persistent context files can be introduced later when the project has enough validated structure to justify them.
